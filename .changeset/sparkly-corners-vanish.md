---
"@valtown/deno-http-worker": major
---

Extend EventEmitter

This makes our event-handling standardized around the Node.js events
system, but also notably changes the call `.addEventListener`
to `.on` or one of the other EventEmitter.
